# =========================
# CORE BACKEND
# =========================
fastapi==0.104.1
uvicorn==0.24.0

pydantic==2.10.0
pydantic-settings==2.3.4
python-dotenv==1.0.1

# =========================
# DATABASE
# =========================
sqlalchemy==2.0.23

# =========================
# SECURITY
# =========================
# NOTE: passlib doesn't appear in your actual installed conda env at all
# (checked against `conda list` -- absent between packaging and
# pathspec). Your auth tests pass using bcrypt directly, so this may be
# an unused leftover. Not removed here since infrastructure/auth.py
# wasn't available to confirm -- paste it if you want this verified and
# trimmed.
passlib[bcrypt]==1.7.4
bcrypt==3.2.2
python-jose[cryptography]==3.3.0
cryptography==41.0.7

# =========================
# LLM (LOCAL)
# =========================
llama-cpp-python==0.2.90
huggingface_hub==0.24.5

# =========================
# UTILITIES
# =========================
numpy==1.26.2
requests==2.31.0
pyyaml==6.0.1
python-json-logger==2.0.7

# NEW (Day 18-19): required by scripts/benchmark_inference.py's memory
# profiling. tracemalloc (stdlib) only sees Python-heap allocations, and
# llama-cpp-python holds model weights + KV cache in native (non-Python)
# memory, so tracemalloc would silently undercount the number that
# actually matters here. psutil's RSS-delta is the correct tool for that
# and adds no compiled/native dependency of its own. Flagged explicitly
# per your request rather than added silently -- drop this line and pass
# --skip-memory to the script if you'd rather not take the dependency.
psutil==5.9.8

# =========================
# DEV / TEST
# =========================
pytest==8.2.2
pytest-asyncio==0.21.1
pytest-cov==4.1.0
# httpx is a hard requirement of fastapi.testclient.TestClient (used
# throughout tests/test_day11.py and tests/test_middleware.py) -- it was
# present in your conda env but missing from this file, meaning a fresh
# `pip install -r requirements.txt` on another machine would fail to
# run the test suite with an ImportError, even though it works locally.
httpx==0.27.2

black==24.4.2
isort==5.13.2
flake8==6.1.0
mypy==1.7.1
bandit==1.7.5
pre-commit==3.5.0

prometheus-client==0.19.0